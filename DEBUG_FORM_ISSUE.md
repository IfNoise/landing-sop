# 🐛 Отладка проблемы с выравниванием данных формы

## Проблема
Данные в Google Sheets смещаются влево:
- **Имя**: OK ✅
- **Ферма**: показывает Email ❌
- **Email**: показывает Телефон ❌
- **Телефон**: пусто ❌
- **Тип фермы**: показывает Сообщение ❌
- **Размер**: пусто ❌
- **Сообщение**: пусто ❌

## Шаг 1: Проверьте что отправляется из браузера

1. Откройте сайт: https://ifnoise.github.io/landing-sop/
2. Нажмите **Ctrl+Shift+R** (жесткая перезагрузка, игнорируя кеш)
3. Откройте консоль браузера (F12)
4. Заполните форму и отправьте
5. **Появится alert** с данными перед отправкой
6. Также проверьте в консоли вывод `console.log('Отправляемые данные:', data)`

### Ожидаемый результат в alert:
```
Имя: Ваше имя
Ферма: Название фермы (или пусто)
Email: ваш@email.com
Телефон: +66... (или пусто)
Тип: greenhouse/indoor/etc (или пусто)
Размер: micro/small/etc (или пусто)
Сообщение: Ваше сообщение
```

## Шаг 2: Проверьте Google Apps Script

1. Откройте ваш Google Apps Script проект
2. Откройте файл с функцией `doPost`
3. **КРИТИЧЕСКИ ВАЖНО**: Добавьте логирование в начале функции `doPost`:

```javascript
function doPost(e) {
  try {
    // ОТЛАДКА: Логируем все что приходит
    Logger.log('===== НАЧАЛО ЗАПРОСА =====');
    Logger.log('postData.contents: ' + e.postData.contents);
    
    // 1. Проверка Content-Type
    if (!e.postData || e.postData.type !== 'application/json') {
      return createErrorResponse('Invalid content type');
    }
    
    const data = JSON.parse(e.postData.contents);
    
    // ОТЛАДКА: Логируем распарсенные данные
    Logger.log('Распарсенные данные:');
    Logger.log('timestamp: ' + data.timestamp);
    Logger.log('name: ' + data.name);
    Logger.log('farm: ' + data.farm);
    Logger.log('email: ' + data.email);
    Logger.log('phone: ' + data.phone);
    Logger.log('farm-type: ' + data['farm-type']);
    Logger.log('farm-size: ' + data['farm-size']);
    Logger.log('message: ' + data.message);
    Logger.log('website: ' + data.website);
    
    // ... остальной код
```

4. **Сохраните** и **задеплойте новую версию** (Deploy → New deployment)
5. Отправьте тестовую форму
6. Проверьте логи: **Executions** → выберите последнее выполнение → **View logs**

## Шаг 3: Проверьте порядок в sheet.appendRow

В функции `doPost`, где вызывается `sheet.appendRow`, убедитесь что порядок такой:

```javascript
sheet.appendRow([
  new Date(data.timestamp),      // Колонка 1: Дата и время
  sanitize(data.name),           // Колонка 2: Имя
  sanitize(data.farm),           // Колонка 3: Название фермы
  sanitize(data.email),          // Колонка 4: Email
  sanitize(data.phone),          // Колонка 5: Телефон
  sanitize(data['farm-type']),   // Колонка 6: Тип фермы
  sanitize(data['farm-size']),   // Колонка 7: Размер фермы
  sanitize(data.message)         // Колонка 8: Сообщение
]);
```

## Шаг 4: Проверьте заголовки в Google Sheets

1. Откройте вашу Google Sheets таблицу
2. Проверьте что в первой строке **ТОЧНО** такие заголовки:

| A | B | C | D | E | F | G | H |
|---|---|---|---|---|---|---|---|
| Дата и время | Имя | Название фермы | Email | Телефон | Тип фермы | Размер фермы | Сообщение |

3. Если заголовки неправильные, запустите функцию `setupSheet()` из меню:
   - **🌾 Landing SOP** → **Настроить таблицу**

## Возможные причины проблемы

### 1. Кеш браузера
**Решение**: Ctrl+Shift+R для жесткой перезагрузки

### 2. Старая версия Apps Script
**Решение**: Deploy → Manage deployments → Edit → Version: New version → Deploy

### 3. Неправильный SPREADSHEET_ID
**Решение**: Проверьте что в Apps Script указан правильный ID таблицы:
```javascript
const SPREADSHEET_ID = 'ВАШ_РЕАЛЬНЫЙ_ID'; // НЕ 'YOUR_SPREADSHEET_ID_HERE'
```

### 4. Неправильное имя листа
**Решение**: Лист должен называться **"Заявки"** (с кириллицей)

### 5. Google Apps Script получает неправильные данные
**Решение**: Проверьте логи (см. Шаг 2) - что именно приходит в `e.postData.contents`

## Что делать дальше?

1. **Сначала проверьте Шаг 1** - появляется ли alert с правильными данными?
   - ✅ Если ДА → проблема в Google Apps Script
   - ❌ Если НЕТ → проблема в script.js (не обновился)

2. **Если alert показывает правильные данные**, но в таблице неправильно:
   - Проверьте Шаг 2 - добавьте логирование в Apps Script
   - Проверьте что `SPREADSHEET_ID` правильный
   - Убедитесь что деплоили новую версию Apps Script

3. **Если alert не появляется**:
   - Очистите кеш браузера полностью
   - Проверьте что файл script.js задеплоен на GitHub Pages
   - Подождите 2-3 минуты для обновления GitHub Pages

## Контрольный список

- [ ] Сделал жесткую перезагрузку страницы (Ctrl+Shift+R)
- [ ] Открыл консоль браузера (F12)
- [ ] Заполнил форму и отправил
- [ ] Увидел alert с данными
- [ ] Проверил console.log в консоли браузера
- [ ] Добавил Logger.log в Google Apps Script
- [ ] Задеплоил новую версию Apps Script
- [ ] Проверил логи выполнения в Apps Script
- [ ] Убедился что SPREADSHEET_ID правильный
- [ ] Убедился что лист называется "Заявки"
- [ ] Проверил заголовки таблицы

---

**После выполнения всех шагов**, напишите результаты:
- Что показывает alert?
- Что показывает console.log в браузере?
- Что показывают логи в Apps Script?

Это поможет точно определить где происходит сбой! 🔍
