# 🚀 Deployment Guide - GitHub Pages

## Быстрый старт

### 1. Настройка GitHub Pages (один раз)

1. Перейдите в **Settings** вашего репозитория
2. В меню слева выберите **Pages**
3. В разделе **Source** выберите: **GitHub Actions**
4. Сохраните изменения

### 2. Первый деплой

```bash
# Добавить все изменения
git add .

# Создать коммит
git commit -m "feat: готов к деплою на GitHub Pages"

# Отправить на GitHub
git push origin main
```

### 3. Проверка деплоя

1. Перейдите во вкладку **Actions** вашего репозитория
2. Дождитесь завершения workflow "Deploy to GitHub Pages" (зелёная галочка)
3. Откройте сайт: `https://ifnoise.github.io/landing-sop/`

---

## Автоматический деплой

✅ **Настроен!** Каждый push в ветку `main` автоматически деплоит изменения.

**Workflow файл:** `.github/workflows/deploy.yml`

---

## Ручной деплой

Если нужно задеплоить вручную:

1. Перейдите в **Actions**
2. Выберите "Deploy to GitHub Pages"
3. Нажмите **Run workflow** → **Run workflow**

---

## Кастомный домен (опционально)

### Для поддомена (blog.example.com):

1. Создайте файл `CNAME` с вашим доменом:
   ```bash
   echo "sop.yourfarm.com" > CNAME
   ```

2. Настройте DNS:
   ```
   Type: CNAME
   Name: sop
   Value: ifnoise.github.io
   ```

### Для apex домена (example.com):

Настройте A-записи на IP GitHub Pages:
```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

---

## Проблемы и решения

### 404 Error
- Проверьте, что `index.html` в корне проекта
- Убедитесь, что Source в Pages = "GitHub Actions"

### Workflow не запускается
- Проверьте права доступа: Settings → Actions → General → Workflow permissions → Read and write permissions

### Изменения не видны
- Очистите кеш браузера (Ctrl+F5)
- Подождите 1-2 минуты после деплоя

---

## Структура проекта

```
landing-sop/
├── .github/
│   └── workflows/
│       └── deploy.yml    # GitHub Actions конфиг
├── .nojekyll             # Отключает Jekyll
├── index.html            # Главная страница
├── styles.css            # Стили
├── script.js             # JavaScript
└── README.md             # Документация
```

---

## Полезные ссылки

- 📖 [GitHub Pages Documentation](https://docs.github.com/en/pages)
- 🔧 [Actions Status](https://github.com/IfNoise/landing-sop/actions)
- 🌐 [Live Site](https://ifnoise.github.io/landing-sop/)

---

**Готово! Ваш лендинг автоматически деплоится при каждом коммите** 🎉
